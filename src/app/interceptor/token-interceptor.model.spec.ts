import { TokenInterceptor } from './token-interceptor.model';

describe('TokenInterceptor', () => {
  it('should create an instance', () => {
    expect(new TokenInterceptor()).toBeTruthy();
  });
});
